좋아, 지금 코드 기준으로 **JWT 파트만 실무 기준으로 깔끔하게 “정리 + 개선본”**만 딱 추려서 정리해줄게.

핵심은 3가지야:

* 🔐 안전한 키 관리
* 🧾 토큰 생성/파싱 책임 분리
* 🧱 Spring Security 연동 가능 구조

---

# ✅ 1. JwtProvider (핵심 정리본)

```java
package com.example.demo.user.jwt;

import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import jakarta.annotation.PostConstruct;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.nio.charset.StandardCharsets;
import java.security.Key;
import java.util.Date;

@Component
public class JwtProvider {

    @Value("${jwt.secret}")
    private String secretKey;

    @Value("${jwt.expiration}")
    private long expiration; // ms 단위

    private Key key;

    @PostConstruct
    public void init() {
        byte[] keyBytes = secretKey.getBytes(StandardCharsets.UTF_8);

        if (keyBytes.length < 32) {
            throw new IllegalArgumentException("JWT secret must be at least 256 bits (32 bytes)");
        }

        this.key = Keys.hmacShaKeyFor(keyBytes);
    }

    // =========================
    // 1. 토큰 생성
    // =========================
    public String createToken(Long userId) {

        Date now = new Date();
        Date expiry = new Date(now.getTime() + expiration);

        return Jwts.builder()
                .setSubject(String.valueOf(userId))
                .setIssuedAt(now)
                .setExpiration(expiry)
                .signWith(key, SignatureAlgorithm.HS256)
                .compact();
    }

    // =========================
    // 2. userId 추출
    // =========================
    public Long getUserId(String token) {

        Claims claims = Jwts.parserBuilder()
                .setSigningKey(key)
                .build()
                .parseClaimsJws(token)
                .getBody();

        return Long.parseLong(claims.getSubject());
    }

    // =========================
    // 3. 토큰 검증 (추가 권장)
    // =========================
    public boolean validate(String token) {
        try {
            Jwts.parserBuilder()
                    .setSigningKey(key)
                    .build()
                    .parseClaimsJws(token);

            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }
}
```

---

# 🔥 핵심 개선 포인트

### 1) Secret Key 안정성 강화

```java
if (keyBytes.length < 32)
```

→ HS256 기준 최소 256bit 보장

---

### 2) 역할 분리

* createToken → 발급
* getUserId → 파싱
* validate → 인증 체크 (필터에서 중요)

---

### 3) 실무 필수 구조 추가

✔ validate() 추가 (필터 안정성 증가)

---

# ✅ 2. JwtAuthenticationFilter (JWT 핵심 연결부)

```java
package com.example.demo.user.jwt;

import com.example.demo.user.security.CustomUserPrincipal;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.Collections;

@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtProvider jwtProvider;

    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain
    ) throws ServletException, IOException {

        String header = request.getHeader("Authorization");

        if (header == null || !header.startsWith("Bearer ")) {
            filterChain.doFilter(request, response);
            return;
        }

        String token = header.substring(7);

        if (!jwtProvider.validate(token)) {
            filterChain.doFilter(request, response);
            return;
        }

        Long userId = jwtProvider.getUserId(token);

        CustomUserPrincipal principal = new CustomUserPrincipal(userId);

        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                        principal,
                        null,
                        Collections.emptyList()
                );

        SecurityContextHolder.getContext().setAuthentication(authentication);

        filterChain.doFilter(request, response);
    }
}
```

---

# 🔥 Filter 핵심 개선

✔ validate 먼저 수행 (예외 차단)
✔ SecurityContext 안전하게 주입
✔ 인증 실패 시 그냥 pass (또는 401 처리 가능)

---

# ✅ 3. application.yml (JWT 설정)

```yaml
jwt:
  secret: "your-very-long-secret-key-at-least-32-bytes-long"
  expiration: 3600000  # 1시간
```

---

# 🚨 실무에서 꼭 추가하는 업그레이드 (중요)

## 1. Refresh Token 구조 (다음 단계)

* Access Token (짧게: 1h)
* Refresh Token (길게: 7~14d)
* Redis 저장

---

## 2. Claims 확장 (권한 추가)

```java
.claim("role", "USER")
```

---

## 3. Bearer 토큰 유틸 분리 (추천)

```java
public String resolveToken(HttpServletRequest request)
```

---

# 🚀 최종 구조 (JWT만 보면)

```
Client
  ↓
JWT Filter
  ↓
JwtProvider (validate + parse)
  ↓
SecurityContext (Principal 저장)
  ↓
Controller (@AuthenticationPrincipal)
```

---

원하면 다음 단계로 바로 이어서:

👉 Spring Security Config (완성형)
👉 Refresh Token + Redis 구조 (실무 핵심)
👉 Role 기반 권한 설계 (USER / ADMIN)

여기까지 하면 “진짜 서비스급 인증 구조” 완성된다.



지금 코드 기준으로 보면 구조는 이미 꽤 잘 잡혀 있고, **“패키지 위치만 정리하면 되는 상태”**야.
로직 자체를 크게 뜯어고칠 단계는 아님.

핵심만 짚어서 정리해줄게.

---

# 1) 지금 상태 평가

현재 구조:

```text
com.example.demo.user.jwt.JwtProvider
com.example.demo.user.security.*
com.example.demo.user.service.UserService
```

👉 문제는 딱 하나

> auth 성격 코드가 user에 섞여 있음

---

# 2) 반드시 해야 할 리팩토링 (최소 변경)

## 🔴 1. JwtProvider 이동

```text
user/jwt ❌
→ auth/jwt ✔
```

---

## 🔴 2. security 패키지 이동

```text
user/security ❌
→ auth/security ✔
```

---

## 🔴 3. 결과 구조

```text
auth
 ├── jwt
 │    └── JwtProvider
 ├── security
 │    ├── JwtAuthenticationFilter
 │    ├── CustomUserPrincipal
 │    └── SecurityConfig

user
 ├── controller
 ├── service
 ├── domain
 ├── repository
 ├── dto
```

---

# 3) UserService는 그대로 둬도 되나?

👉 결론: **지금 그대로 OK**

왜냐:

```java
private final JwtProvider jwtProvider;
```

이건 “사용하는 것”이지 “소유하는 것”이 아님

✔ user service = 로그인 결과로 토큰 발급 요청
✔ auth layer = 토큰 생성 담당

👉 문제 없음

---

# 4) 단 하나 더 개선하면 좋은 부분 (중요)

## ❗ 현재 구조의 작은 냄새

```java
import com.example.demo.user.security.*;
```

```java
@AuthenticationPrincipal CustomUserPrincipal principal
```

👉 이건 아직도 user가 security 구현체를 알고 있음

---

## ✔ 더 깔끔한 구조 (추천)

### auth 쪽에 인터페이스로 숨기기

```java
public interface AuthPrincipal {
    Long getUserId();
}
```

그리고 controller에서는:

```java
@AuthenticationPrincipal AuthPrincipal principal
```

👉 user → security 의존 제거됨

---

# 5) 더 중요한 설계 포인트 (실무 기준)

너 구조는 지금 “모놀리식 + JWT”인데 핵심 원칙은 이거야:

## ✔ auth는 infrastructure layer

```text
user (business)
↑
auth (infra / cross-cutting)
```

---

## ❌ 하면 안 되는 구조

* user가 JWT 생성
* user가 SecurityContext 만짐
* user가 Filter 구현

---

## ✔ 올바른 구조

* auth가 인증 책임
* user는 “업무 로직만”

---

# 6) 한 줄 결론

지금 코드는 이미 방향 맞고,

> ✔ “jwt + security를 합치는 게 아니라 user에서 떼서 auth로 옮기는 단계”만 하면 끝

---

# 7) 다음 단계 (원하면)

여기까지 했으면 다음은 보통 이 순서로 감:

### 1️⃣ Refresh Token 추가 (거의 필수)

### 2️⃣ Access Token 짧게 + Refresh Rotation

### 3️⃣ SecurityConfig Spring Security 6 리팩토링

### 4️⃣ OAuth2 (Google 로그인)

### 5️⃣ Redis 세션/토큰 블랙리스트

---

원하면
👉 “실무급 JWT 구조 (Refresh Token + Redis 포함)”까지 바로 이어서 설계해줄게.




좋아, 지금 코드 기준으로 **“정상 동작 보장되는 수정본”** 크게 하나로 정리해줄게.
(핵심은: 필터 등록 + Stateless + Bean 구조까지 포함)

---

# ✅ 1. JwtAuthenticationFilter (수정본)

```java
package com.example.demo.auth.security;

import com.example.demo.auth.jwt.JwtProvider;
import com.example.demo.user.security.CustomUserPrincipal;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.Collections;

@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtProvider jwtProvider;

    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain
    ) throws ServletException, IOException {

        String header = request.getHeader("Authorization");

        // 1. 토큰 없으면 패스
        if (header == null || !header.startsWith("Bearer ")) {
            filterChain.doFilter(request, response);
            return;
        }

        String token = header.substring(7);

        // 2. 토큰 검증
        if (!jwtProvider.validate(token)) {
            filterChain.doFilter(request, response);
            return;
        }

        // 3. userId 추출
        Long userId = jwtProvider.getUserId(token);

        // 4. Principal 생성
        CustomUserPrincipal principal = new CustomUserPrincipal(userId);

        // 5. Authentication 생성
        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                        principal,
                        null,
                        Collections.emptyList()
                );

        // 6. SecurityContext 세팅 (핵심)
        SecurityContextHolder.getContext().setAuthentication(authentication);

        filterChain.doFilter(request, response);
    }
}
```

---

# ✅ 2. SecurityConfig (필수 수정본)

```java
package com.example.demo.auth.security;

import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {

        http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(session ->
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            )
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(
                jwtAuthenticationFilter,
                UsernamePasswordAuthenticationFilter.class
            );

        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

---

# ✅ 3. CustomUserPrincipal (확실한 형태)

```java
package com.example.demo.user.security;

import lombok.AllArgsConstructor;
import lombok.Getter;

@Getter
@AllArgsConstructor
public class CustomUserPrincipal {
    private final Long userId;
}
```

---

# 🔥 핵심 체크 3개 (이거 안 맞으면 무조건 깨짐)

✔ 1. 필터 등록됨

```java
.addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class)
```

✔ 2. Stateless 설정 있음

```java
SessionCreationPolicy.STATELESS
```

✔ 3. @Component or @Bean으로 필터 생성됨

---

# 💡 결론

이 구조면:

* JWT 인증 정상
* SecurityContext 정상 세팅
* @AuthenticationPrincipal 정상 주입
* userId 정상 사용

---

원하면 다음 단계로
👉 “Refresh Token 구조 (실무형)”
👉 “Security 완전 분리 아키텍처”
👉 “role/authority 추가”

여기까지 바로 확장 가능하다.

